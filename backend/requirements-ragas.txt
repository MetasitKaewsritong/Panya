# Optional dependencies for offline RAGAS evaluation only.
# Runtime chatbot does not require these packages.

ragas==0.2.10
datasets>=2.19.0,<5.0.0
pandas>=2.1,<3.0.0
langchain-openai==0.2.14
langchain-huggingface==0.1.2
scikit-learn>=1.4,<2.0.0
